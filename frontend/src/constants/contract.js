import abi from "../../../backend/artifacts/contracts/Voting.sol/Voting.json"; // Update path if different

export const CONTRACT_ABI = abi.abi;
export const CONTRACT_ADDRESS = "0xYourContractAddressHere"; // <-- Replace with actual deployed address
